# Copy to .env.local for local dev overrides.
# On Railway, set these in the Variables tab.

PORT=3001

# Your Vercel frontend URL — used for CORS allow-list.
# During local dev, the default in index.ts (http://localhost:5173) applies.
CLIENT_ORIGIN=https://your-app.vercel.app
