# Copy this file to .env.local for local overrides.
# On Vercel, set VITE_SERVER_URL in Project Settings → Environment Variables.

# URL of the deployed HandDuel backend (Railway / Render / Fly.io)
# Leave blank to fall back to http://localhost:3001
VITE_SERVER_URL=https://your-backend.up.railway.app
